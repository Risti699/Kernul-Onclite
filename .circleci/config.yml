version: 2.1

jobs:
 jobs:
  build:
    docker:
      - image: ubuntu-2004:202010-01
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      -  name: Running tests
          command: make test 
 
